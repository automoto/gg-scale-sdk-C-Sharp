namespace GGScale
{
    /// <summary>The SDK's own version, reported in diagnostics.</summary>
    public static class SdkVersion
    {
        /// <summary>Semantic version of this SDK build.</summary>
        public const string Value = "0.2.0";
    }
}
